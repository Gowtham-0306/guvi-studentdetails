{
  "name": "be",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
   "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "gow",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.0",       // Add Express
    "body-parser": "^1.20.3",   // Include body-parser if needed
    "cors": "^2.8.5",           // Include CORS if needed
    "dotenv": "^16.4.5",        // Include dotenv for environment variables
    "mongoose": "^8.7.0",       // Include mongoose if using MongoDB
    "nodemon": "^3.1.7"         // Nodemon for development
  }
}
